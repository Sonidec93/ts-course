namespace MyMath {
    export function calculateArea(length: number, breadth: number) {
        return length * breadth;

    }
    export function calculatePerimeter(length: number, breadth: number) {
        return 2 * (length + breadth);
    }
} 