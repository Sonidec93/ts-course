export const PI = 3.14;

export function calculateArea(radius: number) {
    return PI * radius * radius;
}
export default function calculateCircumference(radius: number) {
    return 2 * PI * radius;
}